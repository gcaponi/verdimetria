# Loaded from /etc/nginx/conf.d/ — http context.
limit_req_zone $binary_remote_addr zone=vd_auth:10m rate=5r/m;
limit_req_zone $binary_remote_addr zone=vd_write:10m rate=20r/m;
limit_req_zone $binary_remote_addr zone=vd_general:10m rate=60r/m;
server_tokens off;
